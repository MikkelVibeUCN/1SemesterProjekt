package controller;
import model.*;
import model.product.*;

public class ProductController {
	private ProductContainer productContainer;
	
	/*public ShelfProduct getShelfProduct(int barcode) {
		ShelfProduct shelfProduct = productContainer.getShelfProduct(barcode);
		return shelfProduct;
	} */
	
	/* public UniqueProduct getUniqueProduct(int serialNumber) {
		UniqueProduct uniqueProduct = productContainer.getUniqueProduct(seriaNumber);
		return uniqueProduct;
	} */
}
